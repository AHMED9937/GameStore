{
  "$schema": "https://json-schema.org/schema",
  "$id": "ApiModule",
  "title": "Create an API module library",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "Module name?"
    }
  },
  "required": ["name"]
}
