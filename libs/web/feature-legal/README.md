# feature-legal

This library renders the legal documents from `docs/legal` as public web pages.

## Running unit tests

Run `nx test feature-legal` to execute the unit tests via [Vitest](https://vitest.dev/).
