import type { Metadata } from 'next';

/** TODO(implement-seo): full per-page metadata */
export function buildPageMetadata(_page: string): Metadata {
  return {
    title: 'GameStore',
    description: 'SEO setup — not implemented yet',
  };
}
